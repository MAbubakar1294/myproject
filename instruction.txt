

Make two changes in instructions.txt, at the top and bottom of the file. Make sure that they are separated by at least several unmodified lines.

Run git commit --patch. Using the keystrokes above, commit one of the changes.

Do it again for the other change.

When you’re done, inspect the situation with git log, git status, git diff and git diff --staged.

